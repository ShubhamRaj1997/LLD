class ProductManager(object):
    @staticmethod
    def get_products(query):
        """
        Create a DAO layer and fetch the objects from there, I have created samples for dao directories but have not
        used them as such
        :param query:
        :return:
        """
        # return products from service or DAO layer
        return []