class ExpenseService(object):
    pass

# we can add the functionality of calling the various models by this service and it will interact with all models
# now the only thing is remaining is using the sevrice we can create all the expense objects

# we will have a manager class which will be the orchestator of the whole system and store the data whereas we would
# have another driver class which will do the rest of the work
